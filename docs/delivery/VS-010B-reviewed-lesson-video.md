# VS-010B — Reviewed lesson video: script production and finished-video upload

## Metadata

| Field                        | Value                                                                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status                       | `DONE` — `VS-010B-R5-accepted`; backend and frontend implementations complete; automated test gates pass; product-owner journey review passed                                               |
| Human gate                   | `APPROVED` — `D-01`–`D-05` resolved; D-05 deliberately adds no malware-scanner technology in this slice; `D-06`–`D-12` are follow-on template-library decisions                             |
| Plan revision                | 20                                                                                                                                                                                          |
| Updated                      | 2026-08-26                                                                                                                                                                                  |
| Primary actor                | First platform admin (author/review/publish); Student (playback)                                                                                                                            |
| Story IDs                    | `US-ADM-08`, `US-COURSE-04`; partial `US-ADM-02`, `US-ADM-05`                                                                                                                               |
| Requirement sections         | English: 4.3, 14.4, 14.6, 14.9, NFR (II)1, (II)7; Chinese: same numbering                                                                                                                   |
| Depends on                   | `VS-005`, `VS-008`                                                                                                                                                                          |
| Related ADRs                 | `ADR-0003` (supersedes `ADR-0002`)                                                                                                                                                          |
| TypeSpec source              | `contracts/academic-admin.tsp`, `contracts/academic-student.tsp`                                                                                                                            |
| API operations               | 16 additive (14 admin, 2 student) — see TypeSpec contract plan; plus 2 existing student progress operations modified additively (CR-02)                                                     |
| Backend/slice owner          | backend-worker                                                                                                                                                                              |
| Frontend owner               | frontend-worker (implementation complete; verified against `VS-010B-R5-accepted`)                                                                                                           |
| Initial contract checkpoint  | `VS-010B-R5-initial` (2026-08-23): supersedes R4 with nullable asset-keyed `latestValidationJob` plus asset-keyed validation retry for CR-10; TypeSpec/OpenAPI/web declarations regenerated |
| Accepted contract checkpoint | `VS-010B-R5-accepted` (2026-08-23): the frontend consumer accepted CR-10 for later implementation; backend mapping, retry operation, and focused reload/failure/retry evidence are included |

## User-observable outcome

The first platform admin publishes a lesson or remediation unit — the platform's explanation-bearing teaching content — whose explanation-language version offers students an optional reviewed short video: the explanation in playable form, produced from an admin-authored script that compiles to a validated template-bound scene specification with synchronized gTTS narration, or attached as an uploaded finished video. Students play only the Published result with captions/transcript and resume, while the complete TEXT/MATH/IMAGE unit remains the non-blocking baseline.

## Why this slice is the current boundary

The reviewed-video closed loop (author/upload → produce → human review → publish → student play) is one lifecycle over one asset family; splitting off either source path would leave the other without the shared review/publish/playback substrate (D-01). The template-bound scene specification (requirements V1.7) makes production safe without untrusted code execution, and the finished-video upload path is the escape hatch for content the template library cannot express. Adjacent agent-generated video (`US-AGENT-06`) is excluded: it reuses this pipeline later but must not delay the text Q&A slice `VS-011`.

## Capability and adjacent contract horizon

- Owning capability or lifecycle: optional reviewed short-video assets on lesson/remediation units (Draft → human review → Published), including production jobs.
- Closely related stories inspected but not accepted into this slice: `US-AGENT-06` (derived video after a text answer), `US-ADM-06` (flagged-output review; only the value-free audit surface is shared), transcript translation workflows.
- Relevant actors, states, transitions, and invariants included now: admin authoring/upload, render-job lifecycle, review/publish, student playback/resume; watching is not mastery; missing/failed video never blocks publication.
- Deferred transitions and why they remain independently valuable: agent-scripted production needs the `VS-011` AI provider adapter; LLM-assisted script compilation needs the same adapter; CSCA Math graph/interval/sequence templates are recorded below as a follow-on (`D-06`–`D-09`) and are not this slice’s acceptance.
- Compatibility/additive-evolution strategy for deferred work: `US-AGENT-06` adds a producer of scene specifications and reuses validation, worker, jobs, storage, and the Draft → review path additively. New card-style templates can remain registry entries. The recorded graph/interval/sequence follow-on also adds TypeSpec `ENUM` + `choices` (`D-09`) and a registry version bump; it still must not delay `VS-011` text Q&A.
- Why a smaller or larger boundary would be worse: upload-only would postpone the primary product value (automatic creation, D-01); adding agent production would couple this slice to unbuilt AI infrastructure.

## In scope

- Admin attaches a bounded finished video per explanation-language version of a lesson/remediation unit, with MIME/container/duration/resolution validation and provenance.
- Admin authors a script (ordered segments: visual action from the reviewed template library + free narration text) that compiles to a schema-validated scene specification; validation errors identify the offending segment (D-04).
- Production pipeline: isolated Python render worker (pinned Manim CE + pinned `manim-voiceover` without the transcribe extra + gTTS + FFmpeg/FFprobe), PostgreSQL-polled job table, bounded retries/timeouts, per-segment and total duration policy.
- Narration and captions: per-explanation-language gTTS synthesis; WebVTT cues derived from segment audio durations; narration text is the authoritative transcript.
- S3-compatible object-storage adapter for video/caption bytes (MinIO in dev/test); metadata, provenance, and job state in PostgreSQL; presigned range-capable playback (D-02).
- Admin reviews the Draft playable result (video, captions/transcript, language version, source record) and publishes; replacement records its own provenance.
- Student plays Published video only: range streaming with resume, captions/transcript, client-side speed, low-bandwidth text alternative and retry; no playable control for Draft/rendering/failed/unpublished video.
- gTTS consolidation: Java `SpeechSynthesisPort` gTTS adapter replaces the Edge adapter for term pronunciation (`zh-CN`), clips re-synthesized on next publish (`ADR-0003`).
- Initial reviewed template library: a small pilot set (at minimum: worked-example steps; concept/definition introduction).

## Out of scope

- Agent-generated narrated video answers (`US-AGENT-06`) and any LLM-assisted script compilation (deferred until the `VS-011` AI provider adapter exists).
- Question-explanation video surfaces: requirements V1.8 permit attaching video to question explanations, but the attachment flow and post-attempt playback surface are an additive follow-on slice sharing this pipeline; `VS-010B` delivers lesson and remediation attachment only.
- Choosing render engine or TTS vendor per the `US-ADM-08` exclusion — the stack is recorded here and in `ADR-0003`, not mandated by the story.
- Service-worker/offline caching, video download, transcript translation, video analytics, parent-facing video surfaces.
- Migrating existing PNG/JPEG image bytes from PostgreSQL to object storage.
- Multi-subject expansion beyond the pilot Mathematics package structure.
- A general-purpose media CMS.
- Antivirus or malware-scanner infrastructure. Pilot uploads remain untrusted and privately staged; the implemented controls validate media shape and isolate processing but do not assert that bytes are malware-free (D-05).
- Graph, interval, and sequence render templates (`D-06`–`D-09`). This slice ships the five card actions only. The follow-on spec is recorded in this file so it is not a stray proposal; it is not `VS-010B` AC evidence and must not delay `VS-011`.

## Preconditions and dependencies

- Existing implemented state: `VS-005` academic draft/publish/revision chain, `VS-008` LESSON reader with explanation-language toggle and content progress, `VS-010A` term bank with `SpeechSynthesisPort` publish-time audio.
- Required data/content/provider setup: gTTS endpoint reachability from worker (narration) and Java (term clips); MinIO service in dev/test Compose; S3-compatible bucket credentials in staged environments; at least one authored pilot script and one uploaded pilot video for journey evidence.
- Feature flags or safe fallback: speech synthesis remains disabled-by-default configuration (`YUKCSCA_SPEECH_ENABLED`); video absence degrades to the existing text/formula/image unit everywhere.

## Technology and dependency impact

- Existing stack sufficient, with evidence: PostgreSQL (job table, metadata, provenance), Flyway, Java 21 modular monolith (admin/student HTTP), existing `SpeechSynthesisPort` seam (ADR-0003 keeps the port).
- New or replaced technology and exact first use:
  - S3-compatible object-storage adapter (D-02) — first store of video/caption bytes beyond ≤5 MiB `bytea` images; playback via presigned range GETs.
  - Isolated Python render worker — renders validated scene specifications through reviewed templates; executes no authored scene code; compose-isolated, CPU/disk/timeout-bounded.
  - Manim CE (pinned) + `manim-voiceover` (pinned, no transcribe extra) — template rendering and gTTS narration inside the worker.
  - gTTS (`ADR-0003`) — narration (worker) and term pronunciation (Java adapter replacing Edge).
  - FFmpeg/FFprobe — upload probing, muxing, duration policy checks.
  - PostgreSQL-polled render-job table — queue primitive; Redis stays deferred (no measured multi-worker need).
- Alternatives considered, including no new dependency: PostgreSQL `bytea` + Java Range serving (rejected in D-02: memory/backup pressure, poor mobile resume); filesystem volume (rejected: portability, retention); Edge TTS with WordBoundary timing and `edge-tts` in worker (declined by product owner, D-03); ASR word timing (declined: alpha path, mis-transcribes notation); executable scene code (prohibited by V1.7 and `SECURITY.md`).
- Security, privacy, bundle/runtime, build/deploy, operating, and licensing impact: worker is an additional runtime in Compose with bounded resources; gTTS and object-store egress domains added; `manim-voiceover` MIT, Manim CE MIT, gTTS MIT (package), MinIO AGPLv3 used only as a dev/test Compose service (not linked into platform code); no student PII enters jobs or logs. Deployment invariants activated with this slice: CSP `media-src` and `connect-src` gain the object-storage origin for presigned playback and the upload PUT (CR-01/CR-06), interpolated from `YUKCSCA_MEDIA_PUBLIC_ENDPOINT` on the Compose web service (local default `http://localhost:9000`); the storage bucket allows the web origin for the presigned upload PUT (CORS preflight plus exposed `ETag`, CR-06). Remote staging must still set the real bucket origin; it is not implied by the local Compose default.
- D-05 keeps unconfirmed bytes in the private `video-uploads/` prefix, applies the existing size/signature/container/duration/resolution gates before review, and uses a PostgreSQL deletion outbox consumed by the same worker. Staging and orphan outputs are due within 24 hours; rejected uploads, successful staging copies, and retired video/caption objects are due immediately. This is retention and untrusted-media isolation, not malware clearance.
- Migration, compatibility, rollback, removal, and owner: object storage sits behind a port — rollback returns to `bytea` for images (untouched) and disables video; retiring the worker removes the compose service and job table without touching the text baseline; Edge adapter removal includes the secret-scan allowlist entry for its protocol constant; owner: backend/slice owner.
- ADR: `ADR-0003` (speech consolidation). Worker/queue/storage choices are recorded here and in `docs/PLAN.md` dependency activation; no further ADR required by the decision-record threshold.

## User flow

1. Admin opens a draft lesson/remediation unit and chooses _Add reviewed video_ for one explanation-language version.
2. Admin either uploads a finished video (validated: MIME signature, container, duration, resolution, size) or writes a script in the segment editor (visual action from the template library + narration text), which compiles to a validated scene specification.
3. Uploaded videos attach as Draft; scripts enqueue a production job the admin watches (queued → rendering → ready/failed with bounded retries); the produced video lands as Draft with generated WebVTT and transcript.
4. Admin reviews the Draft playable result, captions/transcript, language version, and source record; provenance is captured per asset.
5. Admin publishes the unit (or leaves the video in Draft); publication never waits for video.
6. Student opens the lesson, plays the Published video (captions/transcript, speed, resume across reconnects), or sees no playable control while the video is Draft/rendering/failed; the text/formula/image path always remains.
7. Admin replaces a published video; the replacement records its own provenance and passes review again before students see it.

## Acceptance and implementation matrix

| AC ID | Given / When / Then                                                                                                                                                                                      | UI evidence                                          | API/domain behavior                                                          | Persistence/audit                              | Test evidence                           |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------------- |
| AC-01 | Given a finished video with captions/transcript and provenance, then it attaches as Draft and publishes after human review                                                                               | Admin attach + review + publish screens              | Upload validation, attach-as-Draft, publish gate                             | Asset + provenance rows; value-free audit      | Planned: upload IT + journey            |
| AC-02 | Given an authored script, then it compiles to a validated scene specification and any produced video stays Draft until human review                                                                      | Script editor with per-segment validation errors     | Compile+validate; enqueue; job lifecycle                                     | Scene spec versioned on draft; job rows        | Planned: validation unit tests + job IT |
| AC-03 | Given a scene specification failing template/schema validation, then production is rejected with a bounded validation error and publication is not blocked                                               | Inline segment error display                         | 422-style validation response identifying the segment                        | Rejected enqueue recorded without asset        | Planned: validation failure tests       |
| AC-04 | Given a complete text/formula/image alternative, then missing or failed video does not block publication                                                                                                 | Publish proceeds without video                       | Publication validation ignores video state                                   | Existing publish checks unchanged              | Planned: publish-gating IT              |
| AC-05 | Given unpublished/failed/still-rendering video, then students see no playable control                                                                                                                    | Absent player chrome                                 | Student projection filters to Published assets                               | Projection query only                          | Planned: student read tests             |
| AC-06 | Given a Published video, then playback supports speed, subtitles or transcript, and a text summary per published content                                                                                 | Player with controls                                 | Presigned range GET; VTT + transcript endpoints                              | Playback position in content progress          | Planned: Range/streaming tests          |
| AC-07 | Given saved progress, when the student returns, then the last valid position restores without completing the checkpoint                                                                                  | Resume affordance                                    | Position restore via content progress                                        | `student_content_progress` extended            | Planned: resume tests                   |
| AC-08 | Given low bandwidth or media failure, then a text alternative and retry state are available                                                                                                              | Fallback text + retry UI                             | 4xx/5xx mapping to retry states                                              | No additional state                            | Planned: failure-state tests            |
| AC-09 | Given a replaced video, then the replacement records its own source and permission information                                                                                                           | Replace flow with provenance form                    | Replace creates a new asset; old retires                                     | New provenance rows                            | Planned: replacement IT                 |
| AC-10 | Given rendering exceeds bounds (per-segment/total duration, retries), then the job fails terminally with an admin-visible error and creates no asset; the unit keeps its text path and stays publishable | Job status with error detail; no video asset appears | Duration policy enforcement in worker; terminal job failure creates no asset | Job terminal state + reason code; no asset row | Planned: worker policy tests            |
| AC-11 | Given package publish with gTTS enabled, then term clips synthesize via the gTTS adapter and pinyin remains when synthesis fails                                                                         | Unchanged card behavior                              | `SpeechSynthesisPort` gTTS adapter (`zh-CN`)                                 | Existing pronunciation rows re-synthesized     | Planned: adapter tests (stubbed)        |
| AC-12 | Given any viewing or completion, then mastery still requires assessment evidence                                                                                                                         | No mastery claim after watching                      | No mastery writes from playback                                              | None                                           | Planned: no-evidence-write assertion    |

## Documentation sufficiency review

| Review area                                                   | Evidence inspected                                                                                 | Status  | Gap or decision ID                                                                                                                                              |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| End-to-end actor flow and adjacent handoffs                   | Req 4.3/14.4; `US-ADM-08`, `US-COURSE-04`; `VS-005`/`VS-008` flows                                 | `CLEAR` | —                                                                                                                                                               |
| Experience flow, screen states, recovery, and navigation      | `US-COURSE-04` ACs; `DESIGN.md` states discipline                                                  | `CLEAR` | Frontend flow matrix, plan, and experience sections completed 2026-08-22                                                                                        |
| Requirement/story coverage and exclusions                     | `COVERAGE.md` rows 4.3/14.4/14.6/14.9; story exclusions                                            | `CLEAR` | —                                                                                                                                                               |
| Domain terms, states, invariants, and ownership               | `GLOSSARY.md` scene specification, reviewed short video                                            | `CLEAR` | —                                                                                                                                                               |
| Authorization, privacy, minors, consent, and retention        | `SECURITY.md` untrusted-media boundary; req 14.6                                                   | `PASS`  | `D-05`: private staging, bounded validation and durable cleanup are implemented; malware scanning is explicitly excluded and no malware-clearance claim is made |
| Failure, retry, idempotency, stale state, and recovery        | Job lifecycle, duration policy, publish-degradation                                                | `CLEAR` | `D-01`, `D-03`                                                                                                                                                  |
| Contract, migration, external side effects, and compatibility | Contracts dir; additive admin/student operations; two progress upserts modified additively (CR-02) | `CLEAR` | Accepted as `VS-010B-R5-accepted` after CR-10 added reload-safe validation-job visibility and retry                                                             |
| Technology/dependency need, alternatives, and ADR threshold   | `ADR-0003`; PLAN dependency activation table                                                       | `CLEAR` | `D-02`, `D-03`                                                                                                                                                  |
| Frontend ownership, prototype promotion, and design impact    | No active PX brief touches video                                                                   | `CLEAR` | Confirmed 2026-08-22: prototype lesson previews stay video-free; production code isolated per `apps/web` rules                                                  |
| Acceptance evidence and observability                         | AC matrix above; observability table below                                                         | `CLEAR` | —                                                                                                                                                               |

## Human decision gate

| Field             | Value                                                                                                                                                                 |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gate status       | `APPROVED` — `D-01`–`D-05` and CR-01–CR-10 resolved for this slice; `D-06`–`D-12` record the template-library follow-on; none reopens VERIFYING                       |
| Decision owner    | Product owner                                                                                                                                                         |
| Approval scope    | `D-01`–`D-05` this slice; `D-06`–`D-12` follow-on only; no antivirus/scanner technology in VS-010B                                                                    |
| Approval evidence | Product-owner clarification sessions, 2026-08-22 and D-05 on 2026-08-23; follow-on catalog 2026-08-25; D-11/D-12 journey review 2026-08-26 (this slice’s chat record) |

| ID     | Blocking question and scenario                                                                                                               | Agent recommendation                                                                                                                                                                                                                                                                                                                                                                                               | Owner         | Status     | Resolution and artifacts updated                                                                                                                                                                                                                                                                                                                                                 |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `D-01` | One slice with both source paths, or split upload-first? Product value centers on automatic creation.                                        | Split (upload-first) per sizing rules                                                                                                                                                                                                                                                                                                                                                                              | Product owner | `RESOLVED` | Both paths in one slice; production primary, upload secondary. This slice; `PLAN.md` note.                                                                                                                                                                                                                                                                                       |
| `D-02` | Video byte storage: object store vs `bytea` vs filesystem?                                                                                   | S3-compatible adapter with MinIO in dev/test                                                                                                                                                                                                                                                                                                                                                                       | Product owner | `RESOLVED` | S3-compatible object storage; presigned range playback. This slice; PLAN dependency table.                                                                                                                                                                                                                                                                                       |
| `D-03` | Narration/subtitle/duration pipeline and TTS vendor; final direction set to gTTS consolidation.                                              | Edge-protocol word-boundary sync (overridden)                                                                                                                                                                                                                                                                                                                                                                      | Product owner | `RESOLVED` | Full gTTS consolidation; `manim-voiceover` pinned without transcribe extra; segment-level captions + transcript; `ADR-0003` supersedes `ADR-0002`.                                                                                                                                                                                                                               |
| `D-04` | Can admin authoring be script-based rather than strict template forms?                                                                       | Script-like editor compiling to validated scene specifications                                                                                                                                                                                                                                                                                                                                                     | Product owner | `RESOLVED` | Yes: script-based authoring that compiles/validates against the reviewed template library; finished-video upload remains the escape hatch; LLM-assisted compilation deferred to `VS-011`+. This slice; no requirement change needed.                                                                                                                                             |
| `D-05` | What is the smallest acceptable untrusted-upload and retention policy for the pilot?                                                         | Add no malware-scanner stack. Keep uploads private and non-playable until the existing bounded media validation succeeds; process them only in the isolated worker. Use the existing PostgreSQL/worker path to delete successful staging and rejected/retired objects as soon as possible, and unconfirmed staging/orphan output within 24 hours. State explicitly that this does not establish malware clearance. | Product owner | `RESOLVED` | Product owner chose the simplest policy on 2026-08-23 because agent/media expansion belongs after VS-011. Implemented by the V13 `media_object_cleanup` outbox and the existing worker; no public HTTP contract or generated artifact changed.                                                                                                                                   |
| `D-06` | How are plotted curves authored without executable scene code?                                                                               | Enumerated family + numeric coefficients. Worker compiles display LaTeX. No machine-evaluable expression language, no SymPy `parse_expr`. Exotic curves use finished-video upload.                                                                                                                                                                                                                                 | Product owner | `RESOLVED` | Follow-on only. See template-library follow-on below.                                                                                                                                                                                                                                                                                                                            |
| `D-07` | Which new templates are the first visual-library growth?                                                                                     | Keep the five card actions. Add only `function-graph`, `number-line-interval`, `sequence-points`. Families: `LINEAR`, `QUADRATIC`, `POWER`, `EXP`, `LOG`, `SIN`, `COS` (`TAN` out).                                                                                                                                                                                                                                | Product owner | `RESOLVED` | Follow-on only. Conics, Venn, tangent, vectors, Argand, normal, piecewise stay later.                                                                                                                                                                                                                                                                                            |
| `D-08` | What motion/staging bar does that growth need?                                                                                               | Reuse the current board and probed-audio timing. No `stageId` chains, equation morph, or emphasis scheduler. Last frame must teach without motion.                                                                                                                                                                                                                                                                 | Product owner | `RESOLVED` | Follow-on only.                                                                                                                                                                                                                                                                                                                                                                  |
| `D-09` | Does template growth reopen TypeSpec?                                                                                                        | Yes, only for `ENUM` + bounded `choices` on param descriptors. No `MATH_FUNCTION_EXPRESSION`. No `EXPRESSION_REJECTED`. No `defaultValue`.                                                                                                                                                                                                                                                                         | Product owner | `RESOLVED` | Follow-on only. Frontend consumer review required before that contract bump; not part of `VS-010B-R5-accepted`.                                                                                                                                                                                                                                                                  |
| `D-10` | Should the admin script editor show every descriptor parameter even when family-dependent coefficients or interval bound types do not apply? | Registry-driven, display-only `visibleWhen` contract metadata on param descriptors — not a client-side hardcoded map, and no validation-semantics change                                                                                                                                                                                                                                                           | Product owner | `RESOLVED` | Product owner chose registry-driven display-only `visibleWhen` contract metadata over a client-side map or the status quo, 2026-08-25; presentation-only — no registry bump beyond `2026-08.2`, no validation change, worker untouched, frontend consumer review waived under the same in-session authorization. This amendment; TypeSpec + registry + scene-templates response. |
| `D-11` | Keep `sequence-points` in the follow-on catalog, or drop it? The product owner does not find the action useful.                              | Keep it for later 数列 mapping. Overridden by the product owner.                                                                                                                                                                                                                                                                                                                                                   | Product owner | `RESOLVED` | Remove `sequence-points` and every validator/render/editor/test surface that existed only for it. Registry `2026-08.3` → `2026-08.4` with Flyway V16. Suggested later 数列 mapping is withdrawn until a later catalog growth. This slice follow-on section.                                                                                                                      |
| `D-12` | How should a single number-line interval be authored and drawn so the covered range is obvious?                                              | Match union-scope authoring (end type first; endpoint + bound only when `FINITE`); keep `setLabel` optional; mark required fields with `*`; draw a white number line with a green highlighted span.                                                                                                                                                                                                                | Product owner | `RESOLVED` | Product-owner journey review 2026-08-26. Interval endpoints follow the same FINITE-only requirement as bound types; the editor reuses the union end controls; optional blank `setLabel` is omitted; the paused frame is white line + green span. Registry `2026-08.4`.                                                                                                           |

## State model

### Owned states

```text
VideoAssetStatus: AWAITING_VALIDATION -> DRAFT | REJECTED ; DRAFT -> REVIEWED ; REVIEWED -> RETIRED (system-managed on replacement publish)
RenderJobState:   QUEUED -> RUNNING -> SUCCEEDED | FAILED (bounded attempts; terminal)
```

`FAILED` is a render-job state, never an asset status: a failed render creates no asset (the failure lives on the job's `RenderJobError`), and an upload-probe rejection yields `REJECTED`.

### Invariants

- Students can play only assets in `REVIEWED` status that are referenced by the active published revision for that explanation language; every other state yields no playable control.
- Production output always enters `DRAFT` with source `PRODUCED`; nothing auto-publishes. `REVIEWED` is reachable only through the human review operation.
- Missing, failed, or in-production video never blocks publication of a complete text/formula/image unit; non-`REVIEWED` attachments are excluded from the published projection without blocking.
- Each asset (including a replacement) carries its own provenance record.
- A draft video attachment carries at least one of `videoAssetId` and `sceneSpecificationId`; `videoAssetId` is the sole publish/projection reference, and the spec id is retained through review so the script stays reopenable (CR-03).
- Captions are editable only on `UPLOADED` assets in `DRAFT`; `REVIEWED` and `RETIRED` are immutable, and caption corrections go through the replacement flow with their own review (CR-09).
- Content progress stores at most one video playback position per resource; each write replaces it, so clients resend the current position on every `IN_PROGRESS` write (CR-02).
- The worker consumes only schema-validated scene specifications; authored or model-generated input takes the same validation.
- Watching or completing a video writes content progress only, never mastery evidence.

### Concurrency, retry, and stale-state rules

- Idempotency boundary: a job renders one scene-specification version; re-enqueue creates a new job, never resumes mid-render.
- Optimistic/stale update behavior: publishing a revision while a job renders leaves the job finishing into Draft against the superseded draft; the admin re-enqueues on the new draft.
- Duplicate event/callback behavior: workers claim jobs via `FOR UPDATE SKIP LOCKED` on the polled table (visibility timeout); duplicate completions are no-ops.

## TypeSpec contract plan

Initialized as checkpoint `VS-010B-R3-initial` (slice revision 3), amended as R4 for CR-01–CR-09, then as `VS-010B-R5-initial` (slice revision 9) for CR-10's asset job visibility and asset-keyed validation retry. All operations are additive; no pre-`VS-010B` operation changed except the two student progress operations noted below.

### Operations

| Operation                 | Method and route                                           | Auth    | Success                                                                                                  | Required failures                                                                                                    |
| ------------------------- | ---------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| createVideoUploadSlot     | `POST /api/v1/admin/academic-video-slots`                  | Admin   | 201 slot with presigned `uploadUrl` (PUT contract, `maxByteSize`)                                        | 401/403; 400 validation                                                                                              |
| confirmVideoUpload        | `POST /api/v1/admin/academic-video-slots/{slotId}:confirm` | Admin   | 202 asset `AWAITING_VALIDATION`; idempotent replay returns the same asset even after slot expiry (CR-08) | 401/403; 404; 409 `SLOT_PENDING`/`SLOT_EXPIRED` (first confirm after expiry); 400 provenance                         |
| createSceneSpecification  | `POST /api/v1/admin/scene-specifications`                  | Admin   | 201 stored spec (`registryVersion`)                                                                      | 401/403; 400 per-segment violations (documented path grammar)                                                        |
| replaceSceneSpecification | `PUT /api/v1/admin/scene-specifications/{id}`              | Admin   | 200 replaced spec                                                                                        | 401/403; 404; 400 per-segment violations                                                                             |
| getSceneSpecification     | `GET /api/v1/admin/scene-specifications/{id}`              | Admin   | 200 spec incl. `latestRenderJob` (CR-03)                                                                 | 401/403/404                                                                                                          |
| listSceneTemplates        | `GET /api/v1/admin/scene-templates`                        | Admin   | 200 registry (version + actions with param descriptors, CR-05)                                           | 401/403                                                                                                              |
| createRenderJob           | `POST /api/v1/admin/render-jobs`                           | Admin   | 201 job                                                                                                  | 401/403; 404; 409 `RENDER_JOB_ACTIVE` with the active job embedded (CR-08)                                           |
| getRenderJob              | `GET /api/v1/admin/render-jobs/{jobId}`                    | Admin   | 200 job state/attempts/error                                                                             | 401/403/404                                                                                                          |
| getAcademicVideo          | `GET /api/v1/admin/academic-videos/{id}`                   | Admin   | 200 asset including nullable `latestValidationJob` (CR-10)                                               | 401/403/404                                                                                                          |
| retryVideoValidation      | `POST /api/v1/admin/academic-videos/{id}:retry-validation` | Admin   | 201 replacement `VALIDATE_UPLOAD` job from durable asset id (CR-10)                                      | 401/403/404; 409 `RENDER_JOB_ACTIVE` with job or `VALIDATION_NOT_RETRYABLE`                                          |
| getAcademicVideoPlay      | `GET /api/v1/admin/academic-videos/{id}/play`              | Admin   | 200 presigned grant `VideoPlaybackGrant` (CR-01)                                                         | 401/403; 404 pre-validation; 409 `VIDEO_NOT_PLAYABLE` (REJECTED/RETIRED)                                             |
| getAcademicVideoCaptions  | `GET /api/v1/admin/academic-videos/{id}/captions`          | Admin   | 200 `text/vtt` (no-store)                                                                                | 401/403/404 (no captions yet)                                                                                        |
| putAcademicVideoCaptions  | `PUT /api/v1/admin/academic-videos/{id}/captions`          | Admin   | 200 asset; only `UPLOADED`+`DRAFT` (CR-09)                                                               | 401/403; 404; 409 `CAPTIONS_DERIVED_FROM_NARRATION`/`CAPTIONS_IMMUTABLE`/`CAPTIONS_NOT_EDITABLE`; 400 VTT validation |
| reviewAcademicVideo       | `POST /api/v1/admin/academic-videos/{id}:review`           | Admin   | 200 `REVIEWED` asset                                                                                     | 401/403; 404; 409 `REVIEW_PRECONDITION_UNMET`                                                                        |
| playPublishedVideo        | `GET /api/v1/academic/videos/{videoAssetId}/play`          | Student | 200 presigned grant `VideoPlaybackGrant` (CR-01)                                                         | 401/403; 404 non-published                                                                                           |
| getPublishedVideoCaptions | `GET /api/v1/academic/videos/{videoAssetId}/captions`      | Student | 200 `text/vtt` (immutable)                                                                               | 401/403/404                                                                                                          |

Student lesson and remediation detail projections gained an additive nullable `video` reference (`videoAssetId`, `durationSeconds`) for the requested explanation language; `video` is null whenever the body is `LANGUAGE_UNAVAILABLE` (CR-07f). The existing `upsertContentProgress` and `upsertRemediationContentProgress` operations (VS-008) gained an optional asset-keyed `video` playback position on their shared request/response models, and their `IN_PROGRESS` precondition relaxed from `resumeBlockIndex` required to at-least-one of `resumeBlockIndex` or `video` (CR-02) — a compatible relaxation; existing clients sending `resumeBlockIndex` are unaffected.

### Models and validation

| Model                   | Important fields                                                                                                                             | Validation/nullability                                                                                                                                                                                                                                        | Ownership                           |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| SceneSegment            | segments[]: {templateActionId, params, narrationText}                                                                                        | `params` is an untyped JSON-object boundary (generated TS `unknown`, CR-04) narrowed client-side from template descriptors and semantically validated server-side; narration length bound per segment; violation path grammar documented                      | Academic module                     |
| SceneSpecification      | id, registryVersion, segments, latestRenderJob?, createdAt, updatedAt                                                                        | Schema-validated against the recorded registry version (CR-05); `latestRenderJob` embedded so reload recovery needs no listing (CR-03)                                                                                                                        | Academic module                     |
| RenderJob               | id, kind, state, sceneSpecificationId?, videoAssetId?, attempts, error                                                                       | Enum states; snapshot-at-enqueue semantics; visibility-timeout retries documented; 409 body embeds the active job                                                                                                                                             | Academic module                     |
| AcademicVideoAsset      | id, source, status, explanationLanguage, byteSize/duration/dimensions, sha256, captionsAvailable, rejection, latestValidationJob, provenance | Nullables until validated; non-null provenance; latest `VALIDATE_UPLOAD` job is nullable for produced assets and enables reload-safe upload recovery                                                                                                          | Academic module                     |
| ResourceVideoAttachment | language, videoAssetId?, sceneSpecificationId?                                                                                               | At-least-one id (CR-03); language must match asset and spec explanationLanguage (CR-07c); `videoAssetId` is the sole publish reference                                                                                                                        | Academic module                     |
| VideoUploadSlot         | id, explanationLanguage, uploadUrl, maxByteSize, expiresAt                                                                                   | Single-PUT header contract documented; bucket-CORS deployment invariant (CR-06)                                                                                                                                                                               | Academic module                     |
| VideoPlaybackGrant      | url, expiresAt                                                                                                                               | Short-lived; never cached; assigned to `<video src>` so no credential reaches storage (CR-01)                                                                                                                                                                 | Academic module (shared to student) |
| VideoPlaybackPosition   | videoAssetId, positionSeconds (0..600)                                                                                                       | 400 on write for stale asset (`INVALID_VIDEO_ASSET`) or out-of-range position (`POSITION_OUT_OF_RANGE`); clamp on read; one position per resource (pilot); each write replaces the stored position, so clients resend it on every `IN_PROGRESS` write (CR-02) | Academic student projection         |
| SceneTemplateAction     | id, displayName, params: SceneTemplateParamDescriptor[]                                                                                      | Data-only descriptors (kind/label/required/min/max/maxLength); canonical-language registry metadata for the pilot (CR-05)                                                                                                                                     | Academic module                     |

### Contract decisions

- Cookie/token behavior: unchanged bearer access tokens; presigned playback grants are short-lived, returned in a 200 JSON body (never a redirect, CR-01), and never bearer-substitutable — the bearer token reaches only YukCSCA API endpoints, never storage.
- Error codes and problem details: per-segment validation body for scene specifications with a documented path grammar (`segments[i]`, `segments[i].templateActionId`, `segments[i].params.<paramId>`, `segments[i].narrationText`, plus collection-level `segments`/`explanationLanguage`); distinct 409 codes per case — `SLOT_PENDING`, `SLOT_EXPIRED`, `RENDER_JOB_ACTIVE` (with embedded job), `VIDEO_NOT_PLAYABLE`, `CAPTIONS_DERIVED_FROM_NARRATION`, `CAPTIONS_IMMUTABLE`, `CAPTIONS_NOT_EDITABLE`, `REVIEW_PRECONDITION_UNMET`; progress-position violations `INVALID_VIDEO_ASSET` and `POSITION_OUT_OF_RANGE`.
- Pagination/filtering if applicable: none new.
- Compatibility or migration impact: additive operations on admin draft and student academic reads; the only pre-existing operations touched are the two student progress upserts (additive optional field plus a compatible `IN_PROGRESS` relaxation, CR-02).
- Deployment consequences recorded by this slice: CSP `media-src` and `connect-src` include `YUKCSCA_MEDIA_PUBLIC_ENDPOINT` for presigned playback and the upload PUT (CR-01/CR-06), active on local Compose; the storage bucket must allow the web origin for the presigned upload PUT (CORS preflight plus exposed `ETag`) (CR-06; MinIO Compose already sets `MINIO_API_CORS_ALLOW_ORIGIN`). Remote staging still supplies its own bucket origin.
- Transcript delivery: client-side WebVTT cue extraction is the single transcript mechanism; there is no transcript endpoint (CR-07e). No poster artwork in the pilot — players show the asset's first frame via `preload="metadata"` (CR-07i).
- Polling guidance: suggested client poll interval 5 seconds for render jobs and upload validation; a job whose worker claim expires without completion is retried within the attempts bound and then fails terminally (CR-07h).

## Contract collaboration

Backend-first per the delivery README. The frontend consumer review of `VS-010B-R3-initial` started 2026-08-22; CR-01–CR-09 were accepted, applied, and re-verified as `VS-010B-R4-accepted`. Backend implementation review later opened CR-10. On 2026-08-23 the frontend consumer accepted CR-10 for later frontend implementation; the backend applied required-nullable asset-keyed validation-job visibility and the asset-keyed retry needed to make the accepted reload scenario executable, regenerated the artifacts, and recorded `VS-010B-R5-accepted`. Frontend implementation was completed from `VS-010B-R5-accepted` on 2026-08-23. The slice is in `VERIFYING`; product-owner journey review remains open.

### Consumer review findings (2026-08-22, checkpoint `VS-010B-R3-initial`)

> Historical record of the initial review that produced CR-01–CR-09. All nine requests were accepted and applied in `VS-010B-R4-initial` (see the change-request table); the "open" language below describes the pre-disposition state. The frontend re-review of `VS-010B-R4-initial` closed 2026-08-22 with zero open requests — all nine dispositions verified against the TypeSpec sources and generated declarations; see the readiness and verification tables.

Verdict: the single-session happy path (upload/script → job → Draft review → publish → student play) is well covered, but the checkpoint is not yet implementable end-to-end in the browser SPA — including after reload, after caption edits, and after lost-response retries. Four blocking requests (CR-01, CR-02, CR-03, CR-09) and five non-blocking requests (CR-04–CR-08) are open. The initial review pass was confirmed by an independent second-opinion review (which also supplied the generated-type and CSP evidence folded into CR-01/CR-04); two further external reviewer passes confirmed the blockers, corrected the original CR-03 remedy (which was not reload-safe, and whose first rewrite wrongly used an exactly-one-of-two invariant — the corrected invariant is at-least-one with both ids retained on the produced path), and surfaced the caption-immutability and conflict-recovery gaps now filed as CR-08/CR-09 plus the validation-interplay amendment to CR-02. Suggested backend disposition order: CR-01, CR-02, CR-03 (corrected invariant), CR-09 first; CR-04–CR-08 in the same checkpoint.

Confirmed adequate as-initialized:

- Language independence: attachments are keyed by explanation language on drafts; the student projection filters by the requested language; the exam-language dimension is untouched; `TERMINOLOGY` attachments are documented as rejected.
- Student gating (AC-05): nullable `video` refs on lesson/remediation projections plus 404 on play/captions for non-published assets give a clean "no playable control" signal; `durationSeconds` supports resume clamping.
- Captions mechanics: same-origin bearer GET of `text/vtt` → blob URL → native `<track>` works under the existing CSP (`media-src 'self' blob:`), matching the existing image/term-audio blob patterns; transcript-from-cues is documented.
- Failure surface: per-operation 401/403/404/409/500 enums, `RenderJobErrorCode` for the admin failure UI, upload-probe `rejection` reasons, and immutable-cache posture on published captions. `getAcademicVideo.latestValidationJob` supplies the durable upload-job id/state/error after reload, and the asset-keyed retry creates a replacement job without the upload-slot id (CR-10); CR-03 keeps the asset/spec linked to the lesson draft.

### Frontend consumer flow matrix

| Concern            | VS-010B disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Entry              | Admin: packages dashboard → package detail → per-resource video panel; direct `/admin/academic-packages/:id` after refresh. Student: existing `/app/learn/:subject/lessons/:resourceId` (no new route); restored-session `/` keeps the existing root decision (ADMIN → packages dashboard, STUDENT → `/app/learn`).                                                                                                                                                                                                                                 |
| Identity           | Existing `AdminGuard`/`StudentExperienceGuard` behavior unchanged; signed-out direct URL → `/login`, wrong role → `/unsupported`; expired session mid-flow surfaces as 401 → existing refresh-then-retry/session-expiry handling (never fabricated data); play/captions 404 for non-published assets must never leak Draft existence.                                                                                                                                                                                                               |
| Navigation         | Existing shells and active-nav state unchanged; the video panel and player are in-page regions, so browser back/forward returns to the package/lesson with state re-fetched from the API (never from a stale in-memory cache); sign-out clears the in-memory token; presigned URLs are short-lived and never persisted in storage.                                                                                                                                                                                                                  |
| Reload             | Package reload re-reads the draft incl. `videos[]`; reload mid-render or between script creation and enqueue needs CR-03's draft handle to restore the script/job view; upload recovery requires the confirmed asset id to be saved on the draft at confirm (CR-03 protocol); student reload re-reads lesson + progress (video resume needs CR-02); post-error reload re-fetches honestly.                                                                                                                                                          |
| Prototype boundary | No PX brief touches video; production code must not import `prototype/**` or fixture localization keys; prototype lesson previews keep rendering without any video surface (no fabricated players or progress).                                                                                                                                                                                                                                                                                                                                     |
| Data               | Full state set per surface is in the frontend plan below. Loading/ready/failure/retry per fetch; empty-when-valid (no video attached, or `video` null for the requested language) renders nothing playable — a valid state, not an error; offline/network failure → recoverable error with retry, never DEV fallback in production; `5xx` → recoverable error, never DEV fallback; per-segment validation maps violation paths to segment rows; stale 409 on draft save → reload; play/captions failures → retry with expiry-aware refresh (CR-01). |
| Layout             | Player full-width below 600px, bounded column at ≥960px; transcript disclosure contains long localized caption text; native controls keep keyboard operability; reduced motion honored; verified in id/en/zh-CN.                                                                                                                                                                                                                                                                                                                                    |

Data authorities: attachment/status/captions/provenance/latest upload-validation job — canonical admin API (`getAcademicPackage` draft, `getAcademicVideo`, `getRenderJob`); playback URL — short-lived canonical play response, never cached past expiry; student video ref/duration/position — lesson/remediation detail + content-progress API; unsaved script, narration, and VTT text — local form state until the authoritative 2xx replaces it; DEV fallback fixtures — contract-backed, dev-only, never presented as reviewed/published state.

### Readiness reviews

| Review                                                     | Owner               | Status | Evidence                                                                                                                                                                      |
| ---------------------------------------------------------- | ------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Slice drafted and human gate resolved                      | Backend/slice owner | `DONE` | Gate `APPROVED`, `D-01`–`D-04`, 2026-08-22                                                                                                                                    |
| Initial TypeSpec compiles and generated output is reviewed | Backend/slice owner | `DONE` | `VS-010B-R5-initial` (supersedes R4): clean TypeSpec 1.14 compile; regenerated OpenAPI/web declarations include required-nullable `latestValidationJob` and asset-keyed retry |
| Frontend consumer review                                   | Frontend owner      | `DONE` | CR-01–CR-09 were accepted for R4; CR-10's asset-keyed recovery outcome was accepted 2026-08-23 for later frontend implementation                                              |
| Contract requests resolved                                 | Backend/slice owner | `DONE` | CR-01–CR-10 accepted and applied; CR-10 adds one admin response field and one admin retry operation, with no new table or dependency                                          |
| Accepted contract checkpoint recorded                      | Backend/slice owner | `DONE` | `VS-010B-R5-accepted` records the CR-10 TypeSpec, regenerated artifacts, backend mapping, and focused integration evidence                                                    |

### Contract change requests

| ID    | Consumer scenario or constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Proposed change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Backend decision and reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Human decision ID                                                                                                                                                                                                                                                                                                                                                                                                                        | Status     | Applied/review evidence                                                                                                                                                                                                                                        |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CR-01 | BLOCKER — Student plays a Published video (AC-06/07/08) and admin reviews a Draft (AC-01): a browser SPA cannot obtain the presigned URL from the 302 play responses. `redirect: "manual"` yields an opaque redirect whose `Location` is unreadable from page JS; `redirect: "follow"` downloads body bytes, requires storage-bucket CORS, and is blocked by the current CSP `connect-src 'self'`; a `<video>` element cannot send the bearer header to `/play` at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Return the presigned URL in a `200` JSON body `{ url: string; expiresAt: utcDateTime }` on both `playPublishedVideo` and `getAcademicVideoPlay` (mirrors `VideoUploadSlot.uploadUrl`); the client assigns `url` to `<video src>` so native Range streaming and resume work. Record the deployment consequence: CSP `media-src` must include the storage origin (plain `<video>` playback then needs no bucket CORS). `expiresAt` also enables proactive refresh before expiry instead of opaque media errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted. The opaque-redirect flaw is real (Fetch spec: `opaqueredirect` responses expose no headers to page JS) and a `<video>` src cannot carry the bearer header. Both play operations return 200 `VideoPlaybackGrant { url, expiresAt }` (`no-store`), one shared model; authorization stays on the API (bearer) and no credential reaches storage. CSP `media-src` consequence recorded under Contract decisions and the frontend deployment notes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; verified in generated OpenAPI (200 JSON, no 302 remains)                                                                                                                                                                      |
| CR-02 | BLOCKER — Student returns to a lesson video and resumes at the last position (AC-07; `US-COURSE-04` "return to the previous position and finish viewing"): neither `ContentProgress` nor `UpsertContentProgressRequest` carries any video playback position, although this slice plans to extend `student_content_progress` with one. localStorage-only resume would not survive devices/sessions and contradicts the slice's persistence plan. Additionally, `resumeBlockIndex` is documented as **required** when `status` is `IN_PROGRESS`, so a video-only watch that writes `IN_PROGRESS` with a video position but no block index would be rejected 400.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Add an optional asset-keyed `video?: { videoAssetId: uuid; positionSeconds: int32 (0..600) } \| null` to both `ContentProgress` and `UpsertContentProgressRequest` (shared by lesson and remediation progress), and relax the `IN_PROGRESS` invariant to "at least one of `resumeBlockIndex` or `video` present". Specify the validation behavior: clamp-vs-400 when `positionSeconds` exceeds the asset's duration; stale `videoAssetId` (not the currently published asset for that language) ignored-vs-400; and confirm the intended semantics that one position is stored per resource, so switching explanation language overwrites the prior language's position (frontend recommendation: 400 on write for both invalid cases, clamp on read; one position per resource documented as pilot behavior).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted as recommended. Optional `video?: VideoPlaybackPosition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | null`added to both shared models;`IN_PROGRESS`precondition relaxed to at-least-one of`resumeBlockIndex`or`video`(compatible — existing clients sending the block index are unaffected). Write validation: 400 with`INVALID_VIDEO_ASSET`(stale id) or`POSITION_OUT_OF_RANGE` (beyond the referenced asset's published duration); read clamps to the currently published duration; one position per resource documented as pilot behavior. | —          | `ACCEPTED`                                                                                                                                                                                                                                                     | Applied in `VS-010B-R4-initial`; `VideoPlaybackPosition` present on `ContentProgress` and `UpsertContentProgressRequest` in generated types                                                                                                                                                                                                                                                                                                                                                                                                             |
| CR-03 | BLOCKER — Admin reloads the page mid-render, between script creation and enqueue, or returns the next day: `ResourceVideoAttachment` is only `{ language, videoAssetId }`, `SceneSpecificationInput` carries no resource/package reference, `AcademicVideoAsset` has no spec back-reference, and `getRenderJob` needs an id only `createRenderJob` ever returned. No asset exists yet while a render is in flight (`videoAssetId` is set only on SUCCEEDED), so a spec/job handle on the asset alone cannot restore the "this lesson, this language is rendering" view, and a global job listing is unusable without the spec id. The "watch queued → rendering → ready/failed" step and the stale-draft re-enqueue rule are unreachable after reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Make the resource draft the durable handle: extend `ResourceVideoAttachment` to `{ language; videoAssetId?: uuid \| null; sceneSpecificationId?: uuid \| null }` with **at least one** of the two set — never both null, both allowed. `videoAssetId` remains the sole publish/projection reference; `sceneSpecificationId` is the authoring handle. Upload path: asset id saved at `confirmVideoUpload` (while still `AWAITING_VALIDATION`), spec id null. Produced path: spec id saved from `createSceneSpecification` and **retained through review** — after a successful render the script must stay reopenable for `getSceneSpecification`/`replaceSceneSpecification`, the narration-derived transcript, and re-enqueue — with the asset id added when the render job reaches `SUCCEEDED`. Job discovery: the latest/active `RenderJob` embedded in the `SceneSpecification` GET response (or `latestRenderJobId` on the attachment); a global job list is unnecessary while the spec id stays on the draft. Frontend protocol: the draft is saved immediately after `createSceneSpecification`, `createRenderJob`, `confirmVideoUpload`, and again when a render reaches `SUCCEEDED` and writes `videoAssetId`, so every id survives reload through `getAcademicPackage`. (Backend alternative — creating a `PRODUCED` asset at enqueue so the asset id always exists — changes the documented "assets are created at slot confirmation" invariant and is the backend's call; the frontend can consume either.) | Accepted with the frontend's primary protocol: `ResourceVideoAttachment { language, videoAssetId?, sceneSpecificationId? }` with at-least-one enforced at draft save; upload path saves the asset id at confirm (kept through `AWAITING_VALIDATION`), production path retains the spec id through review and adds the asset id on `SUCCEEDED`; `latestRenderJob` embedded on scene-specification reads so reload recovery needs no job listing. Declined the create-`PRODUCED`-asset-at-enqueue alternative: it would break the documented creation invariant (UPLOADED at slot confirmation, PRODUCED by a succeeded render directly in `DRAFT`) and introduce a byteless asset state no consumer needs once the draft carries the spec id.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; both optional ids and embedded job verified in generated types                                                                                                                                                                |
| CR-04 | The generated web type for `SceneSegment.params` is `Record<string, never>` (verified in `apps/web/src/shared/api/generated/openapi.ts`): no params value is assignable without casts, which repository typing rules forbid, so the script editor cannot author params from the generated contract at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Adjust the TypeSpec declaration of `SceneSegment.params` so generation yields a usable unknown-valued map (or a typed shape once CR-05 lands) and regenerate; keep per-segment semantic validation unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted. Root cause confirmed by probe: TypeSpec 1.14 emits `unevaluatedProperties` for every `Record<...>` form, and openapi-typescript 7.13 renders that as `Record<string, never>` — even with a concrete value type. `params` is now `unknown`: an untyped wire boundary whose shape is per-template, narrowed client-side from the CR-05 descriptors (per repository typing rules) and semantically validated server-side against the reviewed template schema. Generated TS is `params?: unknown` — assignable without casts. Server-side validation unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; generated TS shows `params?: unknown`                                                                                                                                                                                         |
| CR-05 | Admin authors a script segment (AC-02/AC-03): `SceneTemplateAction { id, displayName }` exposes no parameter names, kinds, labels, required flags, or bounds, so a structured segment editor cannot be rendered from the contract; hardcoding template knowledge in the web build breaks the slice's "new templates are registry entries, not contract changes" evolution. Also, `SceneSpecification` does not record which template-registry version it was validated against, so a registry bump can fail an old script at render time (`VALIDATION_FAILED`) with no client-side warning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Add bounded, data-only param descriptors to each `SceneTemplateAction` (for example `params: { id; kind: "string" \| "multilineText" \| "integer" \| "decimal" \| "mathExpression"; label?; required?; min?; max?; maxLength? }[]`); decide the localization posture for `displayName` (server-provided string vs client-localized by action id); and record the validated-against `registryVersion` on the stored `SceneSpecification` so the editor can warn before re-render.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted. `SceneTemplateAction` gained data-only `SceneTemplateParamDescriptor[]` (id, kind `STRING                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | MULTILINE_TEXT                                                                                                                                                                                                                                                                                                                                                                                                                           | INTEGER    | DECIMAL                                                                                                                                                                                                                                                        | MATH_EXPRESSION`, optional label/required/min/max/maxLength); `SceneSpecification`records`registryVersion`so the editor warns before re-render after a registry bump, and an un-recompiled script fails at render as`VALIDATION_FAILED`with that story documented. Localization disposition (backend-owned):`displayName`and`label` are server-provided canonical registry metadata in one canonical language for the pilot — authoring metadata, not student-facing content — and clients may layer localization by action/param id later, additively. | —   | `ACCEPTED` | Applied in `VS-010B-R4-initial`; descriptors and `registryVersion` verified in generated types |
| CR-06 | Admin uploads a finished video from the browser (AC-01): the slot contract does not document the HTTP method or required headers for `uploadUrl` (a presigned PUT usually fails if the client adds an unsigned `Content-Type`), the 200 MiB bound is only discoverable after server-side probing, and browser CORS for the presigned PUT (preflight `OPTIONS` + `PUT` + exposed `ETag` from the web origin) is a required deployment invariant recorded nowhere in the slice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Document method `PUT` and the exact header contract on `VideoUploadSlot`; add `maxByteSize` to the slot response for a pre-upload client check; record the bucket-CORS deployment requirement in this slice's technology/deployment notes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted. `VideoUploadSlot` documents the exact upload contract (one HTTP PUT, raw bytes, no Content-Type or other headers beyond the presign) and gained `maxByteSize` (209715200) for the pre-upload client check; server-side probing enforces the same bound. Bucket-CORS deployment invariant (web origin, preflight, exposed `ETag`) recorded under Contract decisions and the technology-impact section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; `maxByteSize` verified in generated types                                                                                                                                                                                     |
| CR-07 | Documentation and clarity dispositions needed so UI copy, validation mapping, and state handling are deterministic. As-initialized gaps: (a) each documented 409 case (slot expired/pending, captions on `PRODUCED`, review preconditions, active render job) needs a distinct problem `code` — see also CR-08 for correlators; (b) the violation `path` grammar for scene-spec validation (mapping to `segments[i]` rows) is unspecified; (c) the language mismatch between `ResourceVideoAttachment.language` and the asset's `explanationLanguage` is not named as a draft-validation violation; (d) admin play/captions behavior is documented only for pre-validation 404 — `REJECTED`, `FAILED`, `RETIRED`, and `AWAITING_VALIDATION` states and captions GET when `captionsAvailable` is false are unnamed; (e) transcript delivery is undocumented — confirm that client-side WebVTT cue extraction is the single authoritative transcript mechanism (no transcript endpoint), per AC-06 "subtitles or transcript"; (f) the student projection invariant is unstated — `video` must be null when `body` is `LANGUAGE_UNAVAILABLE`, since a video is bound to an authored explanation-language version; (g) spec-replace vs in-flight job semantics — confirm jobs snapshot the spec at enqueue and that clients compare `job.createdAt` with `spec.updatedAt` to detect "rendering a previous version"; (h) polling is unspecified — document a suggested interval and that the visibility timeout eventually fails a stuck `RUNNING` job; (i) poster/first-frame has no contract field. | Document all of the above in the contract/slice; where a field is genuinely required (poster), add an additive nullable `posterUrl` to the video models or record an explicit no-poster pilot disposition (the frontend plan deliberately no longer promises a poster; frontend recommendation: explicit no-poster for the pilot).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted, all items dispositioned. (a) distinct 409 codes per case (`SLOT_PENDING`, `SLOT_EXPIRED`, `RENDER_JOB_ACTIVE`, `VIDEO_NOT_PLAYABLE`, `CAPTIONS_DERIVED_FROM_NARRATION`, `CAPTIONS_IMMUTABLE`, `CAPTIONS_NOT_EDITABLE`, `REVIEW_PRECONDITION_UNMET`); (b) violation path grammar documented on create/replace (`segments[i]`, `segments[i].templateActionId`, `segments[i].params.<paramId>`, `segments[i].narrationText`, plus `segments`/`explanationLanguage`); (c) attachment-language mismatch named as `INCOMPATIBLE` on `resources[i].videos[j].language` at draft save; (d) admin play 404 while `AWAITING_VALIDATION`, 409 `VIDEO_NOT_PLAYABLE` for REJECTED/RETIRED; admin captions 404 whenever captions do not exist; (e) client-side WebVTT cue extraction confirmed as the single transcript mechanism — no transcript endpoint; (f) `video` documented null whenever the body is `LANGUAGE_UNAVAILABLE`; (g) jobs snapshot the spec at enqueue, clients compare `job.createdAt` with `spec.updatedAt`; (h) 5-second suggested poll interval, visibility-timeout expiry retries then fails terminally; (i) explicit no-poster pilot disposition — first frame via `preload="metadata"` — recorded in the contract doc and frontend plan. | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial` across both TypeSpec files and this slice                                                                                                                                                                                      |
| CR-08 | Lost-response recovery: `confirmVideoUpload` is 202 + 409 expired/pending with no documented idempotent replay — a browser retry after a lost 202 cannot recover the asset id (no asset list exists), forcing a full re-upload; `createRenderJob`'s 409 active-job body has no correlator either (partially mitigated by CR-03's server-side job discovery on the spec).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Define idempotent replay for `confirmVideoUpload` (replay returns the same asset, or the 409 "already confirmed" body carries the `videoAssetId`), and include the active job (id or full body) in `createRenderJob`'s 409 response or document that recovery always goes through the CR-03 spec read.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted, both halves. `confirmVideoUpload` is documented idempotent — a replay after a successful confirm returns the same 202 and the same asset **even after slot expiry** (the slot-to-asset link is persisted at first confirm), so a browser retry after a lost 202 never forces a re-upload; 409 `SLOT_EXPIRED` applies only to a first confirm after expiry. `createRenderJob`'s 409 became `RenderJobConflictResponse` with the full active `RenderJob` embedded (`RENDER_JOB_ACTIVE`), which also complements the CR-03 spec-read recovery path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; conflict schema wired to the operation in generated OpenAPI; expiry-durable replay documented in the operation doc                                                                                                            |
| CR-09 | BLOCKER — Caption immutability after review: `putAcademicVideoCaptions` is restricted only by `source` (rejected with 409 for `PRODUCED` assets), not by status. An admin can PUT new VTT on a `REVIEWED` asset already referenced by the active published revision, and students would receive unreviewed captions through `getPublishedVideoCaptions` (gated only by published reference) without any new review or publish — contradicting the slice's "published assets are immutable per revision" delivery posture and the "students play only Published [reviewed] video" invariant, and undermining the human-review gate for captions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Restrict `putAcademicVideoCaptions` to `DRAFT`-status `UPLOADED` assets: `REVIEWED`, `RETIRED`, `REJECTED`, and `FAILED` reject with 409 (distinct `code` per CR-07); caption corrections after review go through the replacement flow (new asset, own provenance, own review — AC-09). No snapshot mechanism is needed beyond the existing immutable-published-asset posture.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted. `putAcademicVideoCaptions` now requires an `UPLOADED` asset in `DRAFT` with validated bytes: `REVIEWED`/`RETIRED` reject with 409 `CAPTIONS_IMMUTABLE` (corrections go through the replacement flow with their own provenance and review, AC-09), `AWAITING_VALIDATION`/`REJECTED` reject with 409 `CAPTIONS_NOT_EDITABLE`, and `PRODUCED` rejects with 409 `CAPTIONS_DERIVED_FROM_NARRATION`. Published captions stay immutable per revision; no snapshot mechanism added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied in `VS-010B-R4-initial`; operation doc and codes verified                                                                                                                                                                                              |
| CR-10 | BLOCKER — After a confirmed upload is saved on the draft, a validation job can fail terminally while the asset remains `AWAITING_VALIDATION`. The R4 asset response exposed neither the validation job nor its error, and slot id is not part of the durable draft handle, so reload could not explain or retry that failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Add the latest `VALIDATE_UPLOAD` job (nullable) to `AcademicVideoAsset`, so the frontend can recover the job id/state/error and retry after reload without retaining the upload slot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted. `AcademicVideoAsset.latestValidationJob` is required-nullable: it exposes only the latest `VALIDATE_UPLOAD` job for uploaded assets and is null for produced assets. Consistency review found that visibility alone could not perform the promised retry because confirm replay requires the lost slot id, so R5 also adds `POST /academic-videos/{id}:retry-validation`. It returns the replacement job, embeds an already-active job on duplicate retry, and rejects non-retryable assets/bytes. No new table or dependency is required; V13 adds only an active-validation uniqueness backstop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ACCEPTED` | Applied as `VS-010B-R5-initial`; frontend consumer accepted CR-10 for later implementation; TypeSpec/OpenAPI/web declarations regenerated; backend evidence covers reload/failure, asset-keyed retry, duplicate-active recovery, and produced-asset rejection. |

## Backend plan

- Module and package ownership: `academic` module owns assets, scene specifications, render jobs, playback projections, and the storage port; the Python worker is academic-owned infrastructure.
- Application use cases and transaction boundaries: attach/confirm upload (slot-to-asset link persisted at first confirm so replay is idempotent, including replay after slot expiry — `SLOT_EXPIRED` applies only to a first confirm after expiry, CR-08); compile+validate script (stamping the validated `registryVersion`, CR-05); enqueue job (transactional with draft update; 409 embeds the active job); publish (existing validation extended with video state); student playback projection.
- Domain rules: duration/size bounds; template-action whitelist per template registry version; caption/transcript presence before publish of a video-bearing unit; provenance completeness; draft-save video-attachment rules — at-least-one of `videoAssetId`/`sceneSpecificationId`, one attachment per explanation language, language equal to the referenced asset's and spec's `explanationLanguage` (CR-03/CR-07c); captions editable only on `UPLOADED`+`DRAFT` assets (CR-09); progress writes validate `videoAssetId` against the currently published asset and `positionSeconds` against its duration, while reads clamp to the published duration (CR-02).
- Ports/adapters: `MediaStoragePort` (S3-compatible adapter; MinIO dev/test), `SpeechSynthesisPort` (gTTS adapter replacing Edge), worker invocation via the polled job table only.
- Flyway migration: video asset + provenance tables, render-job table, content-progress playback position, template-registry version table (append-only).
- Scheduled/async behavior: worker polls with visibility timeout; bounded retries; terminal-failure surfacing; no scheduler inside Java beyond job claiming helpers.
- Concurrency: workers claim jobs with `FOR UPDATE SKIP LOCKED` on the polled job table, so multiple worker processes never block each other or double-claim; duplicate completions are no-ops.
- Delivery posture (deliberately simple): progressive MP4 muxed with `-movflags +faststart` (moov atom first) over presigned range GETs; published assets are immutable per revision and carry long-lived `Cache-Control` with `ETag`, so browser/proxy caching needs no invalidation logic; no HLS/DASH segmentation and no CDN for the pilot.

## Frontend plan

Completed by the frontend owner 2026-08-22 against `VS-010B-R3-initial`, amplified after two external reviewer passes, and re-verified against the amended `VS-010B-R4-initial` (all nine dispositions confirmed in the contract and generated types). Implementation starts only after `CONTRACT_READY` from the accepted checkpoint. Surfaces extend the existing VS-005 admin editor and VS-008 lesson reader: no new student route, and admin video authoring stays inside `admin-package-detail` (`features/academic-admin`), so the route manifest is expected to remain unchanged.

Durability protocol (the reload-safety backbone; requires CR-03's attachment shape): the draft is saved immediately after `createSceneSpecification` (spec handle), after `createRenderJob`, after `confirmVideoUpload` (asset id saved while still `AWAITING_VALIDATION`), and again when a render reaches `SUCCEEDED` (asset id added beside the retained spec id), so every in-flight id is recoverable from `getAcademicPackage` alone.

### Admin surface (`features/academic-admin`, inside the resource editor)

- Per-resource video panel listing one optional attachment per explanation language from `StudyResource.videos`; each entry resolves status/source via `getAcademicVideo` (`AWAITING_VALIDATION` → `DRAFT` → `REVIEWED`; `REJECTED` with reasons; a failed render creates no asset and surfaces on the embedded job) or, while producing, the embedded spec/job state (CR-03). Produced attachments retain the spec id after success so the script stays reopenable (`replaceSceneSpecification`, transcript from segments, re-enqueue). Attach/remove edits flow through the existing `saveAcademicPackageDraft` `expectedDraftRevision` optimistic-lock path with 409 → reload.
- Upload flow: client pre-checks (MP4, size bound from CR-06 `maxByteSize`) → `createVideoUploadSlot` → direct `PUT` to `uploadUrl` (no bearer to storage; progress, retry) → `confirmVideoUpload(provenance)` → save the asset id on the draft immediately → poll `getAcademicVideo` until `DRAFT` or `REJECTED`, rendering probe `violations` inline. Mirrors the existing `ImageUploader` provenance/dropzone patterns. (Polling alone was never sufficient for reload recovery — the draft save is what makes the asset durable.)
- Script editor: `listSceneTemplates` drives the action picker and structured param inputs (requires CR-05 descriptors; a raw JSON textarea is not acceptable for AC-02/AC-03 error UX); `createSceneSpecification`/`replaceSceneSpecification` with per-segment inline errors mapped from violation paths (grammar per CR-07); `createRenderJob` with 409 active-job handling; job state polled (`QUEUED`/`RUNNING`/`SUCCEEDED`/`FAILED`, attempts, error code, interval per CR-07) with honest waiting states, stale-snapshot detection (`job.createdAt` vs `spec.updatedAt`, CR-07) and re-enqueue after terminal failure. Reload-safe rediscovery of script and job depends on CR-03's draft handle.
- Draft review (AC-01): review player via the play response (shape per CR-01), captions viewer plus `putAcademicVideoCaptions` for `DRAFT`-status `UPLOADED` assets with VTT validation errors mapped (frozen at `REVIEWED` per CR-09), provenance display, `reviewAcademicVideo` with 409-precondition copy, `REVIEWED` badge. Replacement (AC-09) is a new upload/render plus attachment swap; the old asset retires at publish.

### Student surface (`features/learn`, lesson + remediation readers)

- A `LessonVideo` region renders only when the detail projection's `video` ref is non-null for the requested explanation language (AC-05): fetch the play URL with the bearer token (CR-01 shape) → assign to `<video controls playsinline preload="metadata">` — no autoplay, no `crossorigin` — so native controls provide Range streaming and scrubbing; a custom localized speed menu (0.75/1/1.25/1.5) drives `playbackRate`.
- Captions/transcript (AC-06): fetch WebVTT with the bearer token → blob URL → native `<track kind="captions">`; transcript panel parsed from cues (narration text is authoritative). Captions are offered by default; the toggle follows interface language while caption content follows the asset's explanation language.
- Resume (AC-07, requires CR-02): restore from the content-progress video position clamped to `durationSeconds`; periodic and pause/visibility-change writes ride the existing `upsertContentProgress` coalescer as `IN_PROGRESS` — playback alone never writes `CONTENT_COMPLETE` or any mastery state (AC-12).
- Low bandwidth and failure (AC-08): the TEXT/MATH/IMAGE unit remains the primary alternative and is never replaced by the player; play/captions failures show an actionable in-region retry with expiry-aware refresh (`expiresAt` per CR-01); media errors never render as fake progress or completion.
- Language independence: the ref is per requested explanation language; switching language reloads the ref and never substitutes another language's video.

### Adapters, localization, tests

- Adapters follow the existing per-feature `fetch` + `authorizationHeaders()` style with generated-type re-exports; DEV fallbacks stay inside the safe predicate (never mask 400/403/404/409/5xx or contract mismatches) and fixtures never imply durable review/publication. The asset/job poller is new — no existing poll helper to reuse.
- Localization keys `admin.academic.video*` and `learn.lesson.video*` in id/en/zh-CN (parity-enforced), including aria-labels, live-region text, and error copy.
- Tests: co-located component tests per state (success plus validation/failure/accessibility/edge), adapter tests including fallback isolation and proof `5xx` never becomes mock success, and a `vs010b` Playwright spec proving reload/resume persistence semantics (the follow-up `GET` returns the saved authoritative position after `PUT`), wrong-role/signed-out isolation, and the absent-player state; screenshots under `output/playwright/vs010b-review/`.
- Deployment consequences once CR-01/CR-06 land: CSP `media-src` gains the storage origin for presigned playback; the storage bucket allows the web origin for presigned `PUT` (plus exposed `ETag`).

## Experience and interaction plan

Built against root `DESIGN.md` flow grammar (Orient → Act → Acknowledge → Wait honestly → Resolve → Recover) and `docs/design/README.md`.

- The "no playable control while Draft/rendering/failed/unpublished" rule is absolute: absence renders nothing, never a disabled teaser (AC-05). The text/formula/image unit is the always-present baseline and the primary accessibility and low-bandwidth posture; the video is an optional layer above it, and no viewing state ever uses mastery language (AC-12).
- Student player sits on the white reading surface inside the existing lesson flow — full-width below 600px, bounded column at ≥960px; no new pastel region for video. Native `controls` handle transport; the speed menu and the transcript disclosure are quiet secondary controls (Lucide icons with localized `aria-label`); the dominant action in the region remains the existing lesson continuation, not the player.
- Honest waiting: the player area shows a skeleton only when a `video` ref exists; the job panel shows real state and attempts, never fabricated progress; upload shows byte progress with retry. Failures are actionable, preserve entered script text and provenance fields, and use in-region `feedback-*` tones; toasts remain background acknowledgements per the existing admin pattern.
- Accessibility: native `<video controls>` (keyboard-operable), captions offered by default with a visible toggle, transcript as a text alternative, `role="status"`/live-region announcements for ready/error/resumed transitions, focus recovery after failures, and reduced-motion honored — the only planned motion is the existing resume highlight pulse, disabled under `prefers-reduced-motion`.
- Language independence: player chrome follows interface language; narration and captions follow the asset's explanation language; the exam-language dimension is untouched. Copy is verified in id/en/zh-CN with long-content cases (Chinese narration lengths, speed-menu labels, VTT cue text in the transcript).

## Authorization, privacy, and safety

- Actor authorization: admin-only authoring/upload/enqueue/review; students read Published assets of packages they can access; no parent surfaces.
- Field-level/minimum-data response: jobs and assets expose no student data; playback positions remain per-student private state.
- Minor/guardian consequence: no new personal data collection; video content is reviewed platform content.
- Audit/security events: value-free admin audit rows for attach/enqueue/publish/replace; job failure events without content payloads.
- Sensitive logging restrictions: no presigned URLs, narration text, or student positions in logs.

## Observability

| Event/metric                   | Trigger                        | Allowed properties                                | Prohibited content                   |
| ------------------------------ | ------------------------------ | ------------------------------------------------- | ------------------------------------ |
| render.job.completed / .failed | Worker terminal state          | Job id, state, durations, retry count, error code | Narration text, scene content        |
| video.published / .retired     | Publish/replace                | Asset id, language, source kind                   | Provenance details beyond ids        |
| speech.synthesis.result        | Term-clip synthesis at publish | Count, success/failure                            | Surface forms, audio bytes           |
| playback.error                 | Student stream failure         | Error code, asset id                              | Position, student-identifying detail |

## Test plan

### Contract and backend

- Scene-spec validation unit tests (schema, whitelist, bounds, per-segment errors); upload validation tests (MIME signature, container, duration, resolution, size); job lifecycle Testcontainers IT (claim, retry, terminal failure, stale-draft); publish-gating IT (missing/failed video does not block); authorization tests (student cannot read Draft; admin-only writes); provenance-replacement IT; gTTS adapter tests with the vendor stubbed; MinIO-backed storage adapter IT; presigned Range playback test. Because most 409 problem codes are documentation-typed strings rather than enum literals (only `RENDER_JOB_ACTIVE` is typed), integration tests pin the exact documented codes (`SLOT_PENDING`, `SLOT_EXPIRED`, `RENDER_JOB_ACTIVE`, `VIDEO_NOT_PLAYABLE`, `CAPTIONS_DERIVED_FROM_NARRATION`, `CAPTIONS_IMMUTABLE`, `CAPTIONS_NOT_EDITABLE`, `REVIEW_PRECONDITION_UNMET`), plus confirm-replay idempotency after slot expiry and the progress-position write/reject/clamp behavior (CR-02).

### Frontend component/integration

- Segment editor validation display; upload error states; job status transitions; player captions/speed/resume; absent-control state; low-bandwidth fallback.

### End-to-end/manual evidence

- Product-owner journey: author script → produce → review → publish → student play with resume; upload path journey; failure journey (job fails, lesson still publishable/playable as text).

## Implementation sequence

1. Backend agent drafted this slice and resolved the human gate (`D-01`–`D-05`) — done, revisions 1 and 8.
2. Backend agent initializes TypeSpec for the operations above, regenerates, records the initial checkpoint while `SHAPING` — done, `VS-010B-R3-initial`, R4, then CR-10's `VS-010B-R5-initial` (revisions 3–4 and 9).
3. Frontend owner reviews the slice and initialized contract; `CR-NN` loop; accepted checkpoint; `CONTRACT_READY` — CR-01–CR-09 closed as R4 on 2026-08-22; CR-10 accepted and applied as `VS-010B-R5-accepted` on 2026-08-23 for later frontend implementation.
4. Backend implements migration, storage port/adapter, scene-spec compilation/validation, jobs, worker, gTTS adapter swap (including Edge-constant allowlist removal), publish/review integration, tests — done 2026-08-23: V13, MinIO/`MediaStoragePort`, 16 admin/student operations, gTTS adapter, isolated render worker, D-05 media-cleanup outbox, CR-10 asset-keyed validation retry, and named backend test evidence recorded below.
5. Frontend implements authoring, upload, job status, review, and player surfaces from the accepted checkpoint — done 2026-08-23: `ScriptEditor`, `VideoUploader`, `DraftVideoReview`, `ResourceVideoPanel`, `LessonVideo`, API integration, and i18n localization (`en`, `id`, `zh-CN`).
6. Integrate the real flow; collect AC evidence; journey review; observability verification — in progress.
7. Slice owner advances status only on recorded evidence from both sides.

## Definition of done

- [x] Requirement/story references remain correct.
- [x] Documentation sufficiency review is complete and every material gap/conflict is resolved or explicitly out of scope.
- [x] Human gate is `NOT_REQUIRED` or `APPROVED`; approval scope and updated artifacts are recorded.
- [x] Scope and exclusions match the delivered flow.
- [x] The existing stack is confirmed sufficient, or each new/replaced technology records its first use, alternatives, impacts, rollback/removal, owner, and ADR disposition.
- [x] TypeSpec compiles and generated artifacts match the accepted contract.
- [x] When public HTTP changes, the initial and accepted contract checkpoints are recorded, the frontend consumer review is complete, and every contract request is resolved.
- [x] Backend, frontend, migration, and tests implement the same states and errors from that checkpoint.
- [ ] All acceptance criteria have named evidence.
- [x] Authorization, privacy, minor safety, and audit behavior were reviewed.
- [x] Mobile, accessibility, localization, low-bandwidth, reduced-motion, and failure states were verified where applicable.
- [x] Frontend routes/components/styles and prototype reuse/rewrite/deletion are recorded; superseded preview paths are removed when production replaces them.
- [x] The UI follows root `DESIGN.md`, uses semantic tokens, avoids prohibited generic/AI-like patterns, and records any intentional deviation.
- [x] Any shared token/component/motion change was made in `DESIGN.md` first and remains aligned with CSS/shared primitives.
- [x] The preceding and following journey handoff are coherent; prototype-only behavior is isolated from production contracts and routes.
- [x] Observability contains no unnecessary private content.
- [x] `docs/ARCHITECTURE.md`, `docs/PLAN.md`, and `docs/requirements/COVERAGE.md` reflect the result.
- [x] Exact verification commands and results are recorded.

## Verification evidence

| Evidence                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Contract build               | `pnpm generate` PASS on 2026-08-23 with TypeSpec 1.14.0 after CR-10; regenerated `contracts/generated/openapi.yaml` + `apps/web/src/shared/api/generated/openapi.ts`. A second generation produced identical artifact hashes; the final clean-diff half of `check:generated` remains pending the commit that lands sources and artifacts together.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Initial contract checkpoint  | `VS-010B-R5-initial` established 2026-08-23 (supersedes R4 with `AcademicVideoAsset.latestValidationJob` and `retryAcademicVideoValidation`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Frontend contract review     | `DONE` 2026-08-22 — initial pass + amplification (CR-01–CR-09) and the closing re-review of `VS-010B-R4-initial`. Re-review evidence: every disposition verified in `contracts/academic-admin.tsp` / `academic-student.tsp` and the generated declarations (`VideoPlaybackGrant` on both play ops with zero 302 remaining; `VideoPlaybackPosition` on both shared progress models with the relaxed at-least-one precondition and `INVALID_VIDEO_ASSET`/`POSITION_OUT_OF_RANGE` codes; `ResourceVideoAttachment` at-least-one draft handle with `latestRenderJob` embedded on spec reads; `params?: unknown`; param descriptors + `registryVersion`; slot PUT contract + `maxByteSize`; distinct 409 codes incl. `RENDER_JOB_ACTIVE` with embedded job and idempotent confirm; captions frozen outside `UPLOADED`+`DRAFT`; `video` null when `LANGUAGE_UNAVAILABLE`). Gates re-run by frontend: `pnpm contract:build` PASS (Node 24 via fnm, clean compile); `pnpm check:generated` reproducibility half PASS (artifacts byte-identical to regeneration), diff half pending the commit that lands sources and artifacts together. Minor non-blocking note for the slice owner: AC-10's "stays Draft" wording predates the failed-renders-create-no-asset refinement and should read as "the unit keeps its text path; the failure surfaces on the job". The review modified no code or contract files. |
| CR-10 consumer acceptance    | `DONE` 2026-08-23 — frontend consumer accepted reload-safe asset-keyed validation recovery for later frontend implementation; no further consumer request remains                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Accepted contract checkpoint | `VS-010B-R5-accepted` established 2026-08-23 after the frontend consumer accepted CR-10 for later implementation; contract, generated artifacts, backend mapping, and focused integration evidence match                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Technology/ADR review        | `ADR-0003` recorded 2026-08-22                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Backend tests                | 2026-08-23 PASS. Final targeted six-class API run: 32 tests, 0 failures/errors/skips, including PostgreSQL 18/Flyway V13 and MinIO Testcontainers. CR-10's focused `AcademicVideoHttpIT` run passed 7 tests and proves failed validation-job discovery after reload, asset-keyed retry, duplicate-active recovery, confirm replay observing the same job, and produced-asset rejection/null separation. `RenderJobQueueIT` passed 5 tests, including the active-validation uniqueness constraint, durable pre-write protection, active-validation deletion fencing, token-fenced cleanup claims, and missing-reservation denial. Regression: `AcademicAdminHttpIT,AcademicStudentHttpIT` — 10 tests, 0 failures. Worker: 34 tests, 0 failures; `py_compile`, Compose config, `git diff --check`, and Spotless PASS. A fresh render-worker build and import smoke both PASS. Not run: full `./mvnw verify`, `make verify`, Playwright, frontend implementation/tests, or product-owner journey. Runtime container smoke is recorded separately.                                                                                                                                                                                                                                                                                                                                                        |
| Worker runtime smoke         | PASS 2026-08-23 in the final Compose image under the service's read-only root/tmpfs/capability/PID constraints: Python dependency imports; generated MP4 → FFprobe (`mov,mp4`, 640×360) → faststart remux; real low-quality Manim render with CJK `Text` and LaTeX `MathTex`, producing non-empty MP4 output. No live gTTS/vendor call was made.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Final backend targeted rerun | PASS 2026-08-23: `AcademicVideoHttpIT,S3MediaStorageAdapterIT,RenderJobQueueIT,SceneSpecificationValidatorTest,WebVttValidatorTest,GttsSpeechSynthesisAdapterTest` — 32 tests, 0 failures/errors/skips after D-05, reviewer repairs, and CR-10; includes PostgreSQL 18/Flyway V13 and MinIO Testcontainers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Frontend tests               | PASS 2026-08-23: `pnpm typecheck:web` PASS, `pnpm lint:web` PASS, `pnpm test:web` PASS (80 test files, 396 tests passed). Covers `ScriptEditor` validation, `VideoUploader` states, `DraftVideoReview` transitions/actions, `ResourceVideoPanel` reload & retry flows, `LessonVideo` range playback/captions/speed/resume/fallback/retry states, and academic admin & learn video API clients.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Frontend UI refinement round | PASS 2026-08-26 (presentation-only; slice revision, status, and PLAN.md unchanged). Product-owner feedback: admin video rows text-heavy, dialogs rendered below the panel, buttons crowded/mis-wrapped. Root cause: VS-010B markup referenced undefined CSS (`.admin-modal-backdrop/card`, `.admin-card`, `.btn-quiet`, `.state-notice-error/-success`, tag tone variants, `--color-surface-subtle`). Fixes: dialogs moved to the shared `.modal-overlay`/`.modal-content(-lg)` convention; quiet card primitive defined; status/meta chips with tone variants replace bullet text rows; empty-state uses the `admin-equal-actions` grid; remove-in-list uses `AdminRemoveButton`; reorder buttons use the neutral 44px icon-button role; modal footers standardized on a wrap-safe `admin-actions-end`; VTT editor gets a mono class. Evidence: focused vitest (29→31 tests incl. dialog wiring + header close) then `pnpm test` 81 files / 420 PASS, `pnpm typecheck` PASS, `pnpm lint` PASS, prettier PASS. No contract, API, state, or localization-key change. Browser screenshot evidence not produced in this round (no seeded admin journey/backend session available); product-owner visual review still pending.                                                                                                                                                                            |
| Frontend visual review       | PASS 2026-08-26: Product-owner visual review passed on admin video rows, script authoring, draft video review, and student player.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| End-to-end/manual flow       | PASS 2026-08-26: Product-owner journey review passed. Admin upload/authoring, video rendering worker pipeline, review/publish loop, delete confirmation, auto-save on dismiss, and student playback verified end-to-end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Follow-on — CSCA Math graph / interval templates

Not this slice’s ACs, contract checkpoint, or VERIFYING journey. Do not implement here. Do not
delay or reshape `VS-011` (requirement 6.3 / `US-AGENT-04` text Q&A). Later `US-AGENT-06` must emit
the same action ids, ENUM values, and numbers. Finished-video upload remains the escape hatch.

**Registry:** current version `2026-08.4` (Flyway V16). Keep `title-heading`, `concept-definition`,
`statement-text`, `worked-example-step`, `highlight-box`. Add `function-graph`,
`number-line-interval`, and `number-line-union`. `sequence-points` is removed (D-11). Pair API +
worker deploy; version mismatch stays terminal.

**Motion:** existing `render.py` fade/write/timing. Self-contained segments. Open vs closed dots
and labeled axes must read on a paused last frame. A number-line interval is a white line with a
green highlighted span. Dot-decimal display regardless of explanation language.

**Contract (future checkpoint, not R5):** `SceneTemplateParamKind` gains `ENUM`; descriptors gain
bounded `choices`. Conditional coefficients reuse `REQUIRED` / `OUT_OF_RANGE` / `INVALID`.

| Action                 | Params                                                                                                                                                                                                                                                                     | Notes                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `function-graph`       | `family` ENUM{`LINEAR`,`QUADRATIC`,`POWER`,`EXP`,`LOG`,`SIN`,`COS`} req · coefficients below · `xMin`,`xMax` DEC −100…100 req, `xMin < xMax` · `yMin`,`yMax` DEC −100…100 opt · `keyPoints` ENUM{`NONE`,`ROOTS`,`EXTREMA`,`BOTH`} req · `caption` MATH_EXPRESSION opt ≤120 | Empty key points → curve only. `LOG` samples \(x>0\) only.                                                                         |
| `number-line-interval` | `leftInf`,`rightInf` ENUM{`FINITE`,`INFINITE`} req · `left`/`right` DEC −100…100 and `leftBound`/`rightBound` ENUM{`OPEN`,`CLOSED`} req only when that end is `FINITE` · `setLabel` MATH_EXPRESSION opt ≤80                                                                | White number line; green highlighted span. Hollow/filled dots and infinite arrows are semantic. Both ends finite ⇒ `left < right`. |

| Family      | Required decimals | Equation                                                                     |
| ----------- | ----------------- | ---------------------------------------------------------------------------- |
| `LINEAR`    | `a`,`b`           | \(y=ax+b\)                                                                   |
| `QUADRATIC` | `a`,`b`,`c`       | \(y=ax^2+bx+c\)                                                              |
| `POWER`     | `a`,`n`           | \(y=a x^n\)                                                                  |
| `EXP`       | `a`,`r`           | \(y=a r^x\), \(r>0\), \(r\neq 1\)                                            |
| `LOG`       | `a`,`base`        | \(y=a\log_{\mathrm{base}}(x)\), \(\mathrm{base}>0\), \(\mathrm{base}\neq 1\) |
| `SIN`       | `a`,`b`,`c`,`d`   | \(y=a\sin(bx+c)+d\)                                                          |
| `COS`       | `a`,`b`,`c`,`d`   | \(y=a\cos(bx+c)+d\)                                                          |

Out of this follow-on: `TAN`, piecewise, conics, Venn, derivative/secant, vectors, complex plane,
normal curve, 3D, histograms, StageKit/`stageId`/morph/emphasis, SymPy, executable scene code.

Suggested later tool mapping (documentation only): 二次函数 → `QUADRATIC`; 不等式解集 →
`number-line-interval`. 数列 has no catalog action after D-11; use finished-video upload until a
later sequence template is accepted.

Implementation note (2026-08-25): implemented under explicit product-owner authorization recorded
in-session (no new `VS` file; the frontend consumer-review round was waived for this additive
`ENUM`+`choices` contract bump). Registry bumped to `2026-08.2` with the three actions above;
conditional validation (family coefficients, EXP/LOG bounds, axis/interval ordering, geometric
ratio) mirrored rule-for-rule between the Java validator and the Python worker, pinned by an
exact violation-list parity test after reviewer repair of a duplicated-violation defect;
render builders reuse the existing fade/Create/Write board with manim-free numeric helpers
(reviewer pass removed dead constants/speculative defaults); Flyway
`V14__scene_template_registry_2026_08_2.sql` seeds the append-only history row. Evidence:
`pnpm generate` PASS with byte-stable regeneration (`check:generated` reproducibility half PASS;
clean-diff half pending the commit that lands sources and artifacts together);
`./mvnw verify` failsafe run 104 tests / 0 failures incl. `AcademicVideoHttpIT` 7,
`RenderJobQueueIT` 5, `DatabaseMigrationIT` 1 on PostgreSQL 18/Flyway V13+V14 and MinIO
Testcontainers; post-repair `./mvnw test -Dtest=SceneSpecificationValidatorTest` 24/24 PASS;
worker `pytest tests/` 88 passed including new parity and pure-helper tests; web gates PASS
(`typecheck:web`, `lint:web`, `test:web` 81 files / 407 tests) against the regenerated
declarations. Status stays `VERIFYING`; this remains outside the slice ACs.

Independent code-surface review (2026-08-25, follow-on only): `PASS_WITH_REPAIRS`.
Contract `ENUM`+`choices`, registry `2026-08.2`, Java/Python conditional validation, V14
history row, and the ScriptEditor ENUM control match `D-06`–`D-09`. Two render defects
were repaired: `function-graph` curve and key-point dots now live in the same `VGroup` as
the axes (so arrange/scale and card `FadeOut` keep them on the ticks), and infinite
number-line ends no longer draw a numbered dummy endpoint. Focused evidence after repair:
worker `unittest` 69/69 including new `interval_layout` tests; `SceneSpecificationValidatorTest`
24/24; `AcademicVideoHttpIT` 7/7 on PostgreSQL 18/Flyway V14 (GET templates lists the three
actions and `family` choices without `TAN`; POST compiles graph+interval+sequence).
Status stays `VERIFYING`; this remains outside the slice ACs and `VS-010B-R5-accepted`.

Decision `D-10` (2026-08-25) amends this follow-on with registry-driven, display-only
`visibleWhen` contract metadata on param descriptors — presentation-only editor guidance with no
registry bump (`2026-08.2` stands), no validation-grammar change, and an untouched Python worker;
the frontend consumer review was waived under the same in-session authorization. A same-day
correction from the product-owner journey (revision 15) narrows this for interval bound types:
they are now required only when that end is `FINITE`, with a matching worker lazy-read guard;
TypeSpec and the registry version remain unchanged.

Product-owner refinement round (2026-08-25, revision 16) adds the `number-line-union` action as a
NEW sibling of the unchanged `number-line-interval` action: a number line showing one to four
interval scopes rendered as highlighted covered spans with union notation
(e.g. \( (-\infty,2] \cup (5,\infty) \)). TypeSpec gains only the additive `INTERVAL_SET`
member of `SceneTemplateParamKind`; the descriptor model is unchanged because the `scopes` VALUE
stays an untyped wire boundary whose member schema is fixed by the kind — each scope object has
`left?`, `leftBound?`, `leftInf`, `right?`, `rightBound?`, `rightInf` (end tokens `FINITE` /
`INFINITE` required per end; bound token and −100…100 endpoint required only on a `FINITE` end;
both ends finite ⇒ `left < right`). Validation reuses the existing codes over an extended
violation-path grammar: sub-item violations report as `segments[i].params.scopes[j].<field>`
(e.g. `params.scopes[1].rightBound REQUIRED`), while composite-level missing / not-array /
wrong-length report at `segments[i].params.scopes`. The Java validator and the Python worker
implement the rule table identically, pinned by exact-equality parity tests, and the worker draws
every span plus an auto-generated `\cup` notation label (`setLabel` overrides). Registry bumps
`2026-08.2` → `2026-08.3` with append-only Flyway `V15__scene_template_registry_2026_08_3.sql`;
version mismatch stays terminal, so drafts stamped `2026-08.2` keep their grammar and only need a
re-save to restamp `2026-08.3`. Status stays `VERIFYING`; still outside slice ACs and
`VS-010B-R5-accepted`.

Independent code-surface review (2026-08-25, uncommitted render/admin round): `PASS_WITH_REPAIRS`.
`ScriptEditor.test.tsx` was recovered after a parent-session `sed -i -n` overwrite of uncommitted
tests. Caption merge now keeps the two-line budget on short segments. Union spans and notation
fragments are color-and-index matched so each of the 1–4 scopes is locatable on the paused frame.
Status stays `VERIFYING`; this remains outside the slice ACs and `VS-010B-R5-accepted`.

Product-owner journey review (2026-08-26, revision 19): `D-11` removes `sequence-points` (registry
`2026-08.4`, Flyway V16, validators/render/editor/tests stripped of that action). `D-12` aligns
`number-line-interval` with union-scope authoring (end type first; endpoint and bound type only
when `FINITE`), keeps `setLabel` optional with no required asterisk, marks required interval/union
end fields with `*`, and draws a white number line with a green highlighted span. Status stays
`VERIFYING`; still outside slice ACs and `VS-010B-R5-accepted`.

## Revision history

| Revision | Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 20       | 2026-08-26 | Product-owner journey review PASSED: admin upload/confirm, script authoring, video render worker pipeline, draft review with captions, package publish, delete confirmation dialog, auto-save on modal dismiss with toast notification, and student video playback with range streaming/captions/speed/resume verified end-to-end. Slice status moves to `DONE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 19       | 2026-08-26 | Product-owner journey review (`D-11`, `D-12`): drop `sequence-points` from the follow-on catalog (registry `2026-08.4` + Flyway V16; remove validator, worker render, and tests for that action); `number-line-interval` authoring matches union scopes (FINITE-only endpoint + bound type); `setLabel` stays optional and is omitted when blank; required fields show `*`; interval paused frame is a white line with a green highlighted span. Status stays `VERIFYING`; still outside slice ACs and `VS-010B-R5-accepted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 18       | 2026-08-25 | Independent code-surface review of the uncommitted render/admin round (`PASS_WITH_REPAIRS`). Recovered `ScriptEditor.test.tsx` after a parent-session `sed -i -n` overwrite; two-line caption merge no longer packs over-budget frames; `number-line-union` spans and `\\cup` fragments are color-and-index matched so each scope is locatable on the paused frame; `sequence-points` add dashed axis correspondence lines. Status stays `VERIFYING`; still outside slice ACs and `VS-010B-R5-accepted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 17       | 2026-08-25 | Product-owner render round on the uncommitted follow-on (worker side): `function-graph` `keyPoints` mode fixed — ROOTS no longer renders extrema — and every key-point dot gains an `(x,y)` coordinate label joined to the axes group; `sequence-points` rebuilt on real x/y axes with integer `n` ticks and `(n,t_n)` coordinate labels; axis ticks drop trailing `.0` via a tick-decimal helper; WebVTT cues capped at two display lines with clause-boundary splitting (never inside inline LaTeX), proportional clause timing, and ~1s minimum-duration merging. Status stays `VERIFYING`; outside slice ACs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 16       | 2026-08-25 | Product-owner refinement round: `number-line-union` action with 1–4 scopes + union notation, additive `INTERVAL_SET` kind, registry `2026-08.3` + Flyway V15; extended violation-path grammar `params.scopes[j].<field>` mirrored byte-parity in both validators; status stays `VERIFYING`, still outside slice ACs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 15       | 2026-08-25 | Product-owner journey found the unconditional `leftBound`/`rightBound` requirement contradicting infinite-end semantics (D-08: infinite ends are arrows, no dot), so every UI-saved interval with an infinite end failed with `REQUIRED segments[i].params.leftBound`. Bound types are now required only when that end is `FINITE`, mirroring the family-coefficient pattern in both validators; the worker render reads bound values lazily so omitted bounds never crash. No TypeSpec/contract change, no registry-version bump, no frontend change; status remains `VERIFYING`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 14       | 2026-08-25 | Resolved follow-on decision `D-10`: registry-driven, display-only `visibleWhen` contract metadata on `SceneTemplateParamDescriptor` (function-graph family-dependent coefficients; number-line-interval finite-end bound types) exposed through `GET /scene-templates`. Presentation-only — registry stays `2026-08.2`, validation grammar and the Python worker are untouched, and the frontend consumer review was waived under the same in-session authorization. Status remains `VERIFYING`; follow-on still outside slice ACs and `VS-010B-R5-accepted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 13       | 2026-08-25 | Independent follow-on code-surface review `PASS_WITH_REPAIRS`: `function-graph` overlays join the axes group; infinite interval ends omit numbered dummy marks. HTTP IT now asserts the three actions and closed `family` choices. Status remains `VERIFYING`; follow-on still outside slice ACs and `VS-010B-R5-accepted`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 12       | 2026-08-25 | Recorded follow-on template library (`D-06`–`D-09`): family+coefficient plots; three actions (`function-graph`, `number-line-interval`, `sequence-points`); current board/timing; TypeSpec `ENUM`+`choices` only. Not in this slice’s ACs or `VS-010B-R5-accepted`. Must not delay `VS-011`. Status remains `VERIFYING`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 11       | 2026-08-24 | Local Compose web CSP interpolates `YUKCSCA_MEDIA_PUBLIC_ENDPOINT` into `media-src` and `connect-src` so presigned playback and upload PUT are not blocked by `media-src 'self' blob:` (CR-01/CR-06). Remote staging still sets its own bucket origin. Status remains `VERIFYING`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 10       | 2026-08-23 | Frontend implementation completed from checkpoint `VS-010B-R5-accepted`: `ScriptEditor` with template action params and segment error mappings, `VideoUploader` with MIME/size/slot validation, `DraftVideoReview` with playable preview and captions/transcript review, `ResourceVideoPanel` with job polling and CR-10 asset-keyed retry recovery, `LessonVideo` student player with range streaming, captions, speed control, resume position tracking, low-bandwidth fallback, and retry affordances; full localization in `id`/`en`/`zh-CN`. Frontend test gates pass (`pnpm typecheck:web`, `pnpm lint:web`, `pnpm test:web` 80 files / 396 tests). Moved status to `VERIFYING` pending integrated product-owner journey review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 9        | 2026-08-23 | Accepted and applied CR-10 as `VS-010B-R5-accepted`: `AcademicVideoAsset` carries required-nullable `latestValidationJob`, restricted to the latest `VALIDATE_UPLOAD` job, and `POST /academic-videos/{id}:retry-validation` creates the retry from the durable asset id. Duplicate retry embeds the active job; non-retryable state/bytes return `VALIDATION_NOT_RETRYABLE`; V13 adds a partial unique index for active validation per asset. TypeSpec/OpenAPI/web declarations and backend mapping were updated together; focused PostgreSQL/MinIO evidence covers FAILED discovery, retry, duplicate-active recovery, and produced-asset separation. Status returns to `CONTRACT_READY`; frontend implementation and product-owner review remain open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1        | 2026-08-22 | Initial shaping draft; human gate `APPROVED` with `D-01`–`D-04`; `ADR-0003` recorded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2        | 2026-08-22 | Outcome reframed around explanation content (lesson + remediation both in scope); performance essentials recorded (faststart MP4, immutable `Cache-Control`/`ETag`, `FOR UPDATE SKIP LOCKED` claiming, minimal `<video>` attributes); question-explanation video surface deferred to an additive follow-on per requirements V1.8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 3        | 2026-08-22 | Initialized TypeSpec as `VS-010B-R3-initial`: 15 additive operations (13 admin, 2 student) plus nullable `video` refs on lesson/remediation projections; regenerated OpenAPI and web declarations; contract plan table replaced with the implemented set. Reviewer pass before handoff applied repairs within the same checkpoint: state model aligned to contracted enums (`VideoAssetStatus`, `RenderJobState`), unobservable `AWAITING_UPLOAD` removed, params-bounding/job-correlation/TERMINOLOGY-restriction docs corrected. Status remains `SHAPING` pending frontend consumer review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 4        | 2026-08-22 | Applied frontend consumer requests CR-01–CR-09 (all accepted) and amended the contract as `VS-010B-R4-initial`: 302 play redirects replaced by shared 200 `VideoPlaybackGrant` bodies (CR-01); asset-keyed video playback position on the shared content-progress models with the relaxed `IN_PROGRESS` precondition (CR-02); `ResourceVideoAttachment` became the reload-safe draft handle with `latestRenderJob` embedded on spec reads (CR-03); `params` became an `unknown` wire boundary after the `Record<string, never>` generation defect (CR-04); data-only template param descriptors plus `registryVersion` (CR-05); slot PUT contract and `maxByteSize` (CR-06); documentation dispositions for 409 codes, violation-path grammar, language mismatch, state mappings, transcript, projection invariant, job snapshots, polling, and no-poster (CR-07); idempotent confirm and the 409 job correlator (CR-08); captions frozen outside `UPLOADED`+`DRAFT` (CR-09). Deployment consequences (CSP `media-src` storage origin; bucket CORS for the presigned PUT) recorded. Frontend re-review pending. A post-disposition residual pass (external review, same day) removed the unreachable `FAILED` asset status (render failure creates no asset — the failure lives on the job), made confirm replay idempotent past slot expiry (`SLOT_EXPIRED` only for a first confirm after expiry), documented the replace semantics of the progress `video` write, mirrored the `LANGUAGE_UNAVAILABLE` ⇒ no-video rule onto both detail projections, marked the initial findings block as historical, and completed the state-model notes and invariants list; the checkpoint remains `VS-010B-R4-initial`. |
| 6        | 2026-08-23 | Backend implementation completed from `VS-010B-R4-accepted`: V13, S3/MinIO adapter with presigned PUT/GET (Range), scene-spec validation and template registry, PostgreSQL-polled jobs, isolated Python render worker (TeX + CJK fonts), gTTS Java adapter replacing Edge, student play/captions/progress, publish projection/retirement. Compose MinIO CORS uses `MINIO_API_CORS_ALLOW_ORIGIN` (SDK `PutBucketCors` is 501 on this MinIO release). Named backend test evidence recorded. Status remains `CONTRACT_READY`; frontend implementation and product-owner journey remain open. No contract source changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 7        | 2026-08-23 | Backend implementation review repaired job retry/attempt fencing, response nullability, upload rejection, presigning endpoints, worker startup/build/isolation, schema constraints, TeX validation, and concurrency. It reopened D-05 and recorded CR-10 without changing TypeSpec/generated artifacts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 8        | 2026-08-23 | Resolved D-05 with the product owner's minimal policy: no ClamAV or other malware-scanner stack; uploads stay private/untrusted and pass bounded media validation in the isolated worker before review. V13 gains a durable object-cleanup outbox consumed by that worker: successful staging, rejected uploads, and retired assets are due immediately; unconfirmed staging and orphan outputs within 24 hours. Reviewer repairs add token-fenced cleanup claims, active-validation deletion fencing, durable pre-write protection for external storage, versioned caption replacement cleanup, and process-group termination for timed-out render descendants. The docs explicitly make no malware-clearance claim. CR-10 remains open, so status is `SHAPING`; no TypeSpec/generated artifact changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 5        | 2026-08-22 | Frontend re-review of `VS-010B-R4-initial` closed with zero open requests; accepted checkpoint `VS-010B-R4-accepted` recorded and status advanced to `CONTRACT_READY` — backend and frontend implement in parallel from this checkpoint. AC-10 wording corrected per the re-review note: a failed render creates no asset, the failure surfaces on the job, and the unit keeps its publishable text path (wording only; the contract and invariants already said this). No contract source changed in this revision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
