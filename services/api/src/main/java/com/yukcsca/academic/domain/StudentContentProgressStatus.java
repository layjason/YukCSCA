package com.yukcsca.academic.domain;

/** Persisted content-progress statuses. Absent row projects as NOT_STARTED. Never mastery. */
public enum StudentContentProgressStatus {
  IN_PROGRESS,
  CONTENT_COMPLETE
}
